import React from 'react';
import { shallow } from 'enzyme';
import Countdown from './Countdown';
import { expect } from 'chai';

describe('<Countdown>', () => {
  it('should ...', () => {
    // const wrapper = shallow(<Countdown text="test" />);
    // expect(wrapper.text()).equal('test');
  });
});
